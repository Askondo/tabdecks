# Session start ritual — TabDecks

Copy-paste this at the start of every Claude Code session.

---

**Session start — TabDecks**

1. Read `.claude/memory/index.md`.
2. This session's task: [DESCRIBE IN ONE SENTENCE].
3. Relevant memory files to load: [LIST FROM INDEX].
4. Relevant source files to read: [LIST FILES].
5. Before writing any code, quote the relevant function signatures / constants
   from the files above. Do not proceed until you have quoted them.
6. If you cannot find a function or constant in the read files, say so — do
   not invent it.
